import { SafeAreaView } from "react-native";
import { ContactCustomer } from "../../features/customers/ContactCustomer";

export const ContactCustomerScreen = () => {
  return (
    <SafeAreaView>
      <ContactCustomer />
    </SafeAreaView>
  );
};
